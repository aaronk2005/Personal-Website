export type TravelPhoto = { src: string; title: string; location?: string; width?: number; height?: number };

// Add only Aaron's supplied and approved travel photographs to the public album.
export const travelPhotos: TravelPhoto[] = [];
